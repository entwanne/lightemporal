[project]
name = "lightemporal"
version = "0.0.1"
dependencies = [
    "pydantic",
]

[project.optional-dependencies]
dev = [
    "pytest",
]


[tool.setuptools]
packages = ["lightemporal", "test_app"]